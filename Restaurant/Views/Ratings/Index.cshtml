@model IEnumerable<Restaurant.Models.Rating>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RestroID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ratings)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RestroID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ratings)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RateID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RateID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RateID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
